{
  "name": "bhoomichain-land-registry",
  "version": "1.0.0",
  "description": "Blockchain-based Land Registry on Polygon Amoy — College Major Project",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "compile": "hardhat compile",
    "deploy:amoy": "hardhat run scripts/deploy.js --network amoy",
    "deploy:local": "hardhat run scripts/deploy.js --network hardhat",
    "test": "hardhat test",
    "node": "hardhat node"
  },
  "dependencies": {
    "ethers": "^6.11.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@vitejs/plugin-react": "^4.2.1",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.1",
    "vite": "^5.2.0"
  }
}
